# XFishTank for OSX (macports)

This is currently WiP, still not automated the Portfile, but if the patches are
applied manually and the dependencies satisfied, it runs. Even on recent
versions of OSX (as of November 2025).

Originally from FreeBSD ports tree.
